# Backend Environment Variables Example
# Copy this file to your environment directory (e.g., dev/backend.env, staging/backend.env, prod/backend.env)
# and fill in your actual values

# ============================================
# Application Configuration
# ============================================
APP_ENV=development
# Options: development, staging, production

APP_PORT=8080
# HTTP server port

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
DB_HOST=postgres
# Database host address (use service name in Docker, or hostname/IP for external DB)

DB_PORT=5432
# Database port

DB_USER=postgres
# Database username

DB_PASSWORD=postgres
# Database password (change in production!)

DB_NAME=english_coach
# Database name

DB_SSLMODE=disable
# SSL mode: disable, require, verify-ca, verify-full
# Use 'require' or higher in production

# ============================================
# Redis Configuration
# ============================================
REDIS_HOST=redis
# Redis host address (use service name in Docker, or hostname/IP for external Redis)

REDIS_PORT=6379
# Redis port

REDIS_PASSWORD=
# Redis password (leave empty if no password is set)

REDIS_DB=0
# Redis database number (0-15)

# ============================================
# JWT Configuration
# ============================================
JWT_SECRET=change-me-in-production-use-strong-random-string
# JWT secret key for token signing (MUST be changed in production!)
# Generate a strong random string: openssl rand -base64 32

# ============================================
# Logging Configuration
# ============================================
LOG_LEVEL=debug
# Logging level: debug, info, warn, error
# Use 'debug' for development, 'info' or higher for production

# ============================================
# CORS Configuration
# ============================================
# Note: CORS_ALLOWED_ORIGINS can be set here or in config.yaml
# If not set, defaults to: http://localhost:3000,http://localhost:5173
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
# Comma-separated list of allowed origins
# Use '*' to allow all origins (not recommended for production)

